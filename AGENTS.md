Get explicit psyche approval before changing a skill or role.

Curriculum owns only `skills/*.md`, `roles.datom`, and repository documentation.
Do not add runtime, deployment configuration, generated consumer output, or a
parallel legacy representation here.

A skill carries no heading naming the skill or containing the word "skill".

A skill's description lives in that skill file's own frontmatter, not restated elsewhere.

No `- ` hyphen-space bullet prefixes; one directive per line as plain prose.

Put contributor and agent rules here; user guidance in README.md; durable design
in ARCHITECTURE.md; temporary agent workarounds in NON_IDEAL_AGENTS.md; and
local rationale beside the data it explains.
